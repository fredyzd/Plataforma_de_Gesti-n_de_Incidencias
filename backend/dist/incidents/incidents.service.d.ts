import type { AuthzUser, IncidentCommentRecord, IncidentRecord, TrackingEventRecord } from './incidents.types';
import type { CreateIncidentDto } from './dto/create-incident.dto';
import type { UpdateIncidentDto } from './dto/update-incident.dto';
import type { AssignIncidentDto } from './dto/assign-incident.dto';
import type { ChangeStatusDto } from './dto/change-status.dto';
import type { CreateCommentDto } from './dto/create-comment.dto';
import type { ListIncidentsDto } from './dto/list-incidents.dto';
import { NotificationsService } from '../notifications/notifications.service';
import { EventsGateway } from '../events/events.gateway';
export declare class IncidentsService {
    private readonly notifications;
    private readonly events;
    private readonly incidents;
    private readonly trackingEvents;
    private readonly comments;
    private sequence;
    constructor(notifications: NotificationsService, events: EventsGateway);
    private nowIso;
    private slaDeadline;
    private isSlaBreached;
    getAllIncidents(): IncidentRecord[];
    private assertAgentRole;
    private canAccessIncident;
    private getOrFailIncident;
    private nextTicketNumber;
    private pushTracking;
    private assertVersion;
    createIncident(user: AuthzUser, dto: CreateIncidentDto): IncidentRecord;
    listIncidents(user: AuthzUser, filters: ListIncidentsDto): IncidentRecord[];
    getIncidentById(user: AuthzUser, incidentId: string): IncidentRecord;
    updateIncident(user: AuthzUser, incidentId: string, dto: UpdateIncidentDto): IncidentRecord;
    assignIncident(user: AuthzUser, incidentId: string, dto: AssignIncidentDto): IncidentRecord;
    changeStatus(user: AuthzUser, incidentId: string, dto: ChangeStatusDto): IncidentRecord;
    getTracking(user: AuthzUser, incidentId: string): TrackingEventRecord[];
    listComments(user: AuthzUser, incidentId: string): IncidentCommentRecord[];
    addComment(user: AuthzUser, incidentId: string, dto: CreateCommentDto): IncidentCommentRecord;
}
